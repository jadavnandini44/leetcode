from typing import List

class Solution:
    def canJump(self, nums: List[int]) -> bool:
        maxIndex = 0
        n = len(nums)
        
        for ind in range(n):
            if ind > maxIndex:
                return False
            maxIndex = max(maxIndex, nums[ind] + ind)
        
        return True
